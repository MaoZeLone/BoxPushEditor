---
description: 新功能或改方案必须先定方案，用户确认后才能写正式代码
alwaysApply: true
---

# 先定方案再开发

新功能、改玩法、改数据模型、改编辑器流程、或有多种做法时：先出方案，等用户明确同意，再改代码 / 资产 / 配置。

## 方案阶段（只讨论，不落地）

- 先写清：要解决什么、选哪种做法、关键数据或接口、界面怎么用、这次不做哪些。
- 有明显分叉就并列利弊，让用户挑；不要自己拍板开写。
- 可以读代码、列现状、画流程；不要新建正式实现、不要顺手重构。

## 什么算确认

用户说「开发」「实现」「按这个做」「定了」「就这样」等，才进入正式开发。

不算确认：只问细节、只说「嗯」、只改需求的一部分、还在提反例。

## 可以直接动手（不必再出方案）

- 已定方案里的后续实现或收尾
- 明确的修 bug、改文案、改样式、改一条配置
- 按 skill 写当天修改记录、只加 Cursor 规则

## 反例

- ❌ 用户说「我们先定方案」，下一句就开始改 DA / C++ / Slate
- ❌ 方案里还写着「也可以另一种」，没选就动工
- ✅ 方案对齐后用户说「开发」，再按已定范围实现
